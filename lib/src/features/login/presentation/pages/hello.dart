import 'package:flutter/material.dart';

class HolaApi extends StatelessWidget {
  const HolaApi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const Center(child: Text('hola papi')));
  }
}
